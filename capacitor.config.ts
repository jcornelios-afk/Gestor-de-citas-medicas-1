import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Gestor de citas medicas',
  webDir: 'www'
};

export default config;
